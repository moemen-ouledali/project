import { HighlighTitleDirective } from './highligh-title.directive';

describe('HighlighTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighTitleDirective();
    expect(directive).toBeTruthy();
  });
});
